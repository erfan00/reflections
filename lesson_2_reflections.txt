It initializes an empty git repository. Git would not do it for you.

It is an intermediate stage between the working directory and the repository. It offers those files that are prepared to be commited.

First make the command of git status, and then see what has been prepared to be committed.

When I want to different version or a new demo version of my program, it would be helpful if I use branches.

The diagrams can help me directly see the branch structure.

After merging, the master branch contains all the features from both the two branches. 

automatic merging: Pros: do not need to do it by myself.
					Cons: may lead to serious mistake.